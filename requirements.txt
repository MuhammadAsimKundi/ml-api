# requirements.txt  (top of file)
--only-binary=:all:
--extra-index-url https://download.pytorch.org/whl/cpu          # CPU wheels are smallest

Flask==2.3.2
Werkzeug==2.3.8
torch==2.2.2+cpu
torchvision==0.17.2+cpu
Pillow==9.5.0
numpy==1.26.4
